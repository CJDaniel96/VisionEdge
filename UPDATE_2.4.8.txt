VisionEdge 2.4.8 累積更新包（可由 2.4.6 或 2.4.7 更新）
停止 VisionEdge，將包內檔案按原目錄覆蓋，再重新啟動並 Ctrl+F5。
不含資料庫、runtime_data、相機設定檔或使用者圖片。

新增各 Label 即時分數條、門檻標線、分數/門檻數字與 OK/NG 命中狀態。
可從「調整 Label 門檻」進入取像與樣板修改；儲存後依原流程套用。
新增「放大畫面」，支援全螢幕與原始/檢測結果切換；縮小或 Esc 可退出。
預設選擇檢測結果；相機尚未準備好時暫用原始預覽，就緒後恢復結果畫面。
比對框線由 2px 加粗到 4px，參考位置框由 1px 改為 2px。
斷線、待套用或無有效結果時不顯示舊 Label 分數。
包含 2.4.7 相機設定頁、產品刪除、清空流程修正。

驗證：瀏覽器預設結果、全螢幕進出及原始畫面切換；live_detail_test、i18n_static_test、rule_group_roundtrip_test 通過。
此輪使用模擬影像驗證，未連接 Qualcomm 實機。
