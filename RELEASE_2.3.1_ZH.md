# VisionEdge 2.3.1：精細標記縮放

修正相機抓圖只能縮小適應視窗、難以精細框選的操作缺口。

## 操作

1. 流程設定選產品，按「從相機擷取」。
2. 影像上方按 100% 看原圖大小；小目標可用 200%、＋／−，或 Ctrl＋滾輪。
3. 放大後用捲軸移動，或按「拖曳平移」再左鍵拖動；中鍵也可直接拖曳。
4. 左鍵畫框前先關掉「拖曳平移」。填樣板名稱／角色候選／門檻，再畫框、指派及加入。
5. 「適合視窗」回到整張圖。新擷取影像會自動回到適合視窗。

縮放不更改來源圖片與原圖 ROI 座標。100% 為原圖像素對應 CSS 像素；200% 不會增加原始細節。畫布記憶體不會隨 200% 顯示增加到四倍原圖，避免不必要的記憶體使用。影片先暫停或擷取後再標記。

## 驗證

- 1920×1080 合成相機影像，100% 下框選後經實際「加入」API 保存：x=700,y=450,w=60,h=40。
- 同圖 200% 下框選後保存：x=850,y=500,w=30,h=20，確認 CSS 放大正確換算回原圖。
- 平移拖曳不新增框；瀏覽器無 JavaScript 錯誤。
- JavaScript 語法、Flow Designer、Label/Flow regression、多語系靜態測試通過。

本版只修改標記介面，未加入箱子旋轉校正，也未改變原有檢測演算法。真機、節拍與現場準確度仍需驗收。

## 升級

先結束或中止工件並停止錄影與服務，備份現場資料。更新程式和 static，保留 db、uploads、runtime_data、config/edge.ini 與憑證，不以範例設定覆蓋現場設定。重新啟動並重新整理頁面；應看到縮放工具列。

docs 目錄附完整繁體中文／西班牙文工程師手冊：16 章，包含逐欄位設定、樣板建立、門檻調校、單張及包裝案例、異常測試與維護。HTML 可離線閱讀及瀏覽器列印。
