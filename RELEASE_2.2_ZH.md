# VisionEdge 2.2.0

這版以 2.1.0-edge.9 為基礎，保留免登入與原有通用檢測流程，未加入 APL 治具分類、固定 ROI、SIFI 或外部上傳。

## 日常操作

1. 在流程設定建立產品、擷取樣板並儲存套用。
2. 啟動相機，在「拍照判定」輸入序號（預設選填），按拍照判定。
3. 系統保存原圖、結果圖與規則分數。重新拍照會新增紀錄。
4. 按「結束此件」後處理下一件；不會把待確認或不合格改成合格。
5. 從「檢測履歷」搜尋序號，開啟原圖、結果或 JSON 明細。

「儲存畫面」只存一般照片；「拍照判定」才建立檢測履歷。單張判定不代表 SOP 所有步驟已執行完成，原有即時 SOP 流程仍獨立運作。

合格（OK）沿用產品既有 ANY/ALL、樣板門檻與 NG override 規則；明確命中 NG 樣板時為不合格（NG）；其餘未通過情況為待確認（UNKNOWN），需調整後重拍。此版沒有新增通用信心分數門檻，也沒有宣稱完成光線補償。

## 本版變更

- 主画面與流程設定統一為 Apple 風格淺色介面：留白、圓角卡片、系統字體、藍色主要操作與清楚狀態。
- 進階步驟條件預設收合；掃碼必填可選擇啟用，不需要帳號。
- 序號在首張成功保存後鎖定；重整或伺服器重啟仍保留尚未結束的產品。
- 原圖、結果圖、判定與分數在同一筆 SQLite 交易保存，保存失敗不顯示成功。
- 拍照使用獨立樣板快取，不污染即時 SOP 狀態；設定版本改變時拒絕判定。
- 拍照請求支援識別碼去重；同一請求重試不產生重複履歷。
- 修正樣板尚在加入時立刻儲存，可能覆蓋新步驟的操作競態。

## 更新與資料位置

先停止舊服務並備份整個舊專案。將本版程式、static 與版本文件更新至部署目錄，保留現場的 config/edge.ini、憑證、db、uploads 與 runtime_data，不要用預設設定覆蓋現場設定。

新履歷位於 runtime_data/inspection_history.sqlite3，包含影像 BLOB；首次執行自動建立。備份前先停止服務，連同原有資料一起備份。履歷不自動清除，請依現場容量安排備份；容量不足時系統拒絕建立新紀錄。

啟動方式與 edge.9 相同。未更新硬體相機、串流協定或既有部署依賴。

## 驗證範圍

- 新增 inspection_history_test.py：真實 SQLite / 樣板比對，驗證保存、去重、重拍、序號鎖定、重開資料庫恢復、UNKNOWN、明確 NG、序號必填、相機未就緒、設定版本與空間不足。
- 原有 edge_consistency、edge_smoke、visionedge_regression、label_flow_regression、flow_designer、live_flow_graph 測試。
- 瀏覽器實際建立產品、框選樣板、儲存套用、拍照判定、重整恢復、履歷查詢與響應式排版檢查。
- 相機測試使用合成影片；QTI 實機與 4K 效能仍須在現場設備驗證。
