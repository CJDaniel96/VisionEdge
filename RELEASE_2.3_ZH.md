# VisionEdge 2.3.0 — 影像包裝循環

本版在 2.2.2 多語系版本上新增自動包裝循環，保留免登入與簡潔淺色介面，未加入 APL。

## 操作流程

工作區清空 → 空箱就位 → 依序放入 A、B 等物品 → 最後齊套確認 → 取走整箱 → 自動等待下一箱。

第一次啟動或相機中斷後，必須先讓系統看到清空的工作區。相機啟動時若已有裝好的箱子，不會直接認定為新的一箱。固定治具模式則在清空後放入第一個物品時開始。

## 初次設定

1. 開啟「流程設定」，選擇產品，勾選「用影像自動開始與換箱」。
2. 選擇「箱子或托盤會取走」或「固定治具，只取走物品」。
3. 擷取清空畫面，框住整個放箱區或治具內部，將樣板「設為循環條件／工作區清空」。不可只選一小塊在放料前後都不變的背景。
4. 箱子模式另擷取空箱內部作為「空箱就位」。放入物品後，這個條件應不再成立。
5. 選擇放料後仍可見的箱緣或治具特徵，設為「定位特徵」。這個特徵被遮住時會暫停步驟判定。
6. 為 A、B 各建立步驟，在正確位置框選物品樣板。OK／NG 與組內 ANY／ALL 規則仍可使用。
7. 勾選最後仍應看得到的物品。會被後續包材蓋住的早期物品可以取消「完成時仍須看得到這一步的物品」，但至少保留一個最後驗證步驟；早期步驟仍須實際通過並留下證據。
8. 設定穩定秒數、最後確認秒數及位置容許偏移，再「儲存並套用」。檢測更新率至少每秒 2 次。

包裝模式會把步驟樣板與循環樣板都限制在原位置附近，margin 必須大於 0。這是樣板搜尋位置的容許偏移，不是通用的物件外框包含判斷；應用正確位置與故意錯位的實拍影像調整。

## 判定與異常

- 所有必要步驟依序完成、最後指定物品仍可見且無 NG，持續穩定後才提示取走。
- 在箱子仍可確認存在時，最後物品消失或 NG 出現會取消齊套狀態。
- 正向辨識到工作區清空並穩定，才結束本箱。單純沒有辨識到箱子不代表取走，避免遮擋觸發換箱。
- 提前取走記為「未完成即取走」；停止、斷流、重啟或中止不會記為合格。
- 同一箱留在畫面不會反覆計數。保存失敗時停止循環，不顯示合格。
- 進行中的箱子會鎖定相關設定修改。需要放棄時用「異常處理／中止此箱」，清空後重新開始。

## 履歷與升級

「檢測履歷／自動包裝紀錄」可查看開始、步驟完成、警報、齊套確認、取走或中止事件，以及原圖、結果圖、明細。紀錄存於 `runtime_data/edge/packaging_history.sqlite3`，請將整個 runtime_data 納入備份；影像逐箱累積，需規劃磁碟容量。

自動模式使用箱號追蹤，尚未與手動拍照的序號／掃碼鎖定流程整合。既有手動模式保留，啟動自動模式前須先結束手動檢測。

本壓縮檔不包含你的產品資料、影片或履歷。升級前停止舊服務並備份；可將新版程式套用到原安裝目錄，保留原本 db、uploads、runtime_data 與設備設定。資料表會在啟動時建立，既有產品預設不啟用自動包裝。

## 驗證範圍

已完成 13 項循環測試、真實樣板匹配的合成影像整合測試、舊功能回歸、多語系靜態檢查，以及瀏覽器操作與事件證據檢查。涵蓋錯位、跳步、遮擋、提前取走、齊套失效、斷流與紀錄保存失敗。

尚未使用現場相機、現場包材或 Qualcomm QTI 硬體驗證。此功能依賴樣板和可見定位特徵，不是通用手部辨識；被遮住的物品只能證明先前步驟曾通過，不能證明之後仍存在。上線前需用實物驗證光線、移位、反光、手部遮擋與空箱／滿箱的區別，調整 ROI、門檻與穩定時間。
