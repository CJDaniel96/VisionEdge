# VisionEdge 2.2.2：群組判定與操作簡化

以使用者提供的 2.2.1 多語系版為基礎，保留淺色介面、四語系、免登入及現有檢測功能。參考 TM-app 的群組分層方式，將組內與組間條件分開呈現。

## 操作

在「流程設定」選擇產品，下方檢測步驟區會顯示「整張畫面怎樣才算合格？」：

- **全部檢查組都通過**：例如螺絲與標籤都是必要檢查，兩組都通過才合格。
- **任一完整檢查組通過**：例如外觀 A 與外觀 B 都可接受，任一整組通過即可。

每個步驟也是一個檢查組；其右側選單決定「組內任一 OK 樣板符合」或「全部 OK 樣板在同一畫面符合」。從「選擇 OK / NG 樣板」加入樣板。

任何啟用組的 NG 樣板命中，仍優先判為不合格。停用的組不參與判定。單張畫面的合格條件不會取代 SOP 依序完成所有必要步驟的規則。

## 修正

- SOP 定義 API 現在會回傳整體判定模式；流程編輯器載入、儲存與重新載入均保留模式。
- 舊版客戶端儲存時若未傳判定模式，保留資料庫既有值，不再默默改為 ALL。
- 無效判定模式拒絕儲存，原有設定保持不變。
- 新增控制項、範例與提示支援繁中、簡中、英文及西班牙文。
- 原先已被舊版覆蓋的設定無法自動推知；請依產品需求重新選擇一次並儲存。

## 驗證與更新

新增 rule_group_roundtrip_test.py，以真實 SQLite 及樣板比對驗證 ANY/ALL 差異、遺漏欄位保留、錯誤輸入不修改資料、NG 優先及停用組排除。原有流程、標註、多語系、Edge 一致性及檢測履歷測試通過。瀏覽器驗證繁中與英文的載入、切換、儲存與回讀。

部署前停止舊服務並備份。更新程式與 static，保留現場 config、憑證、db、uploads、runtime_data。壓縮檔不含測試資料。相機硬體及 QTI/4K 效能未於本次重測。
